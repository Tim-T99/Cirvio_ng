services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cirvio_dev
    ports:
      - "5432:5432"
    volumes:
      - cirvio_db_data:/var/lib/postgresql/data

volumes:
  cirvio_db_data:
