export const environment = {
  production: true,
  apiUrl: 'https://cirviong-production.up.railway.app',
  // Public Google OAuth client ID. Empty string disables the
  // "Sign in with Google" button (gated via !!environment.googleClientId).
  // Set to the production OAuth client ID to enable it.
  googleClientId: '',
};
