export const environment = {
  production: false,
  apiUrl: 'http://localhost:4000',
  // Set GOOGLE_CLIENT_ID in your Google Cloud Console OAuth credentials
  // and replace the empty string below (or inject via build config)
  googleClientId: '',
};
